apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: low-priority
value: 100
globalDefault: false
description: "Low-priority batch workloads — safe to evict at any time (inventory-sync-job)"
